#!/usr/bin/env nu
def --wrapped main [
  --branch: string = "nixpkgs-unstable",
  --out: string
  pkg: string
  ...args: string
] {
  let $outlink = $out | default $pkg
  nix build --out-link $outlink $"github:nixos/nixpkgs/($branch)#($pkg)" ...$args
}
