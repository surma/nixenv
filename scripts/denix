#!/usr/bin/env -S nu --stdin

def main [...files: path] {
    for file in $files {
        let tmp = $"($file).tmp"
        cp $file $tmp
        rm $file
        mv $tmp $file
        chmod u+w $file
    }
}
