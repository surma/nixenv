#!/usr/bin/env nu

def --wrapped main [
	--nixpkgs: string = "release-25.05"
	file: string
	...args: string
] {
	nix-build --expr $"let nixpkgs = builtins.getFlake ''github:nixos/nixpkgs/($nixpkgs)''; pkgs = import nixpkgs {}; in pkgs.callPackage \(import ($file)\) {}" ...$args
}
